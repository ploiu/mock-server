{
  "compilerOptions": {
    "strict": true
  },
  "fmt": {
    "exclude": [
      "README.md",
      "src/ui/AccordionElement.min.js",
      "src/ui/.vite/**/*.js",
      "src/ui/.vite/**/*.ts",
      "src/ui/vite-env.d.ts",
      "src/ui/dist/**/*",
      "src/ui-old/**/*",
      "generated/**/*"
    ],
    "singleQuote": true,
    "useTabs": false,
    "indentWidth": 2,
    "proseWrap": "preserve"
  },
  "lint": {
    "exclude": [
      "src/ts/Deno.d.ts",
      "src/test/uiTests.js",
      "generated",
      "src/ui/AccordionElement.min.js",
      "src/ui/.vite/**/*.js",
      "src/ui/.vite/**/*.ts",
      "src/ui/vite-env.d.ts",
      "src/ui/dist/**/*",
      "src/ui-old/**/*"
    ]
  },
  "tasks": {
    "dev": "export MOCK_SERVER_ENV=dev; deno run --watch -A ./src/ts/MockServer.ts & deno run -A --node-modules-dir npm:vite dev",
    "vite-build": "deno run -A --node-modules-dir npm:vite build",
    "test": "deno fmt; deno test --allow-net",
    // starts up the server with a test config file and a frontend with the testing script baked in. You will need to open the browser to the server page for the tests to run
    "ui-test": "rm -f config-test.json; deno fmt; deno run --allow-read --allow-write ./src/ts/GenerateUICode.ts --test; deno run --allow-read --allow-write --allow-net ./src/ts/MockServer.ts --load-ui --config ./config-test.json",
    // build the ui and runs the backend server for local testing and development
    "run": "deno task generate-ui; deno run --allow-read --allow-write --allow-net --allow-env=MOCK_SERVER_ENV ./src/ts/MockServer.ts --load-ui",
    // bundles the UI files into the main application
    "generate-ui": "deno fmt; deno task vite-build; deno run --allow-read --allow-write ./src/ts/GenerateUICode.ts",
    // installs this application onto your system path
    "install": "deno task generate-ui; deno install --allow-read --allow-write --allow-net --allow-env=MOCK_SERVER_ENV --force ./src/ts/MockServer.ts",
    // company intranet proxies can cause issues with TLS. use this option ONLY if you suffer these errors, they cannot be resolved otherwise, AND IF YOU KNOW WHAT YOU ARE DOING
    "install-unsafe": "deno task generate-ui; deno install --unsafely-ignore-certificate-errors --allow-read --allow-write --allow-net --allow-env=MOCK_SERVER_ENV --force ./src/ts/MockServer.ts",
    "check": "deno fmt; deno lint",
    "types": "deno types > ./src/ts/Deno.d.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@^0.225.1",
    "@std/cli": "jsr:@std/cli@^0.224.0",
    "@std/fmt": "jsr:@std/fmt@^0.224.0",
    "@std/fs": "jsr:@std/fs@^0.224.0",
    "@std/http": "jsr:@std/http@^0.224.0",
    "@vitejs/plugin-vue": "npm:@vitejs/plugin-vue@^5.0.4",
    "primevue": "npm:primevue@^3.52.0",
    "vite": "npm:vite@^5.2.10",
    "vue": "npm:vue@^3.4.26",
    "vue-router": "npm:vue-router@^4.3.2"
  }
}
